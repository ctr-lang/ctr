__Description__: Should be able to import YAML data via require. Technically, we don't use the `yaml` method here since `require-yaml` parsed the yaml file for us. Its just another use case demenstration.