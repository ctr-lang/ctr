__Description__: Testing the memoizing.

__Notes__

+ Once the transform Function is called the returning results are then memoized so next time around its called there is no need to transform the data again.