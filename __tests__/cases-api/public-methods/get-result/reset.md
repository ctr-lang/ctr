__Description__: By default `getResult` resets the style set once called but this action can be overridden via the `reset` option

__Notes__

+ `getResult(reset = true, raw = false)`
+ `getResult({reset, raw})`