__Description__: If the users defines a string as their `query` value then assume they would like to create a `at` mixin with said value

Notation
```
ctr({
  media: {
    <...>: <...>
    query: <string>
  }
})
```