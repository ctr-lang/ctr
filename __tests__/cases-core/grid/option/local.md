__Description__: Within the `grid` object a user should be able to specify a `global` or `option` object to specify defaults

__Notes__

- The weight for these defaults are as follows `option > global > default`