//alias: 'top-left', 'tl', 'top'

//flex
.test
  ctr({
    grid: {
      align: {
        location: 'top-left'
      }
    }
  })

ctr('.test', {
  grid: {
    align: {
      location: 'top-left'
    } 
  }
})

//non-flex
.test
  ctr({
    grid: {
      align: {
        location: 'top-left'
        flex: false
      }
    }
  })


ctr('.test', {
  grid: {
    align: {
      location: 'top-left'
      flex: false
    }
  }
})

//flex
.test
  ctr({
    grid: {
      align: 'top-left'
    }
  })

ctr('.test', {
  grid: {
    align: 'top-left' 
  }
})

//non-flex
.test
  ctr({
    grid: {
      align: 'top-left' false
    }
  })


ctr('.test', {
  grid: {
    align: 'top-left' false
  }
})



// alais
.test
  ctr({
    grid: {
      align: 'tl'
    }
  })


ctr('.test', {
  grid: {
    align: 'tl'
  }
})

