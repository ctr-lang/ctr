__Description__: Should be able to merge into a a `CtrClass` instance

__Notes__

- While you can do this I recommend you `extend` a `CtrClass` rather than `merge` a Stylus object. I'm still fleshing this thought out