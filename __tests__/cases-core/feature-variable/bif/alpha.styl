.test
  ctr({
    $$: {
      base: red
      bg: "alpha($base$, 0.2)"
    }
    background: "$bg$"
    color: alpha(red, 0.2)
  })

ctr('.test', {
  $$: {
    base: red
    bg: "alpha($base$, 0.2)"
  }
  background: "$bg$"
  color: alpha(red, 0.2)
})