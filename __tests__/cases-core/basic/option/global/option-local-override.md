__Description__: Locally specified options within `ctr` instances should override the global default options

__Notes__

+ You want to do this before any `ctr` instances
+ These defaults can also be set in JS
