//yml start
ctr:::setOption: {
  type: {
    rootSize: 18px
    lineHeight: {
      minSize: 1em
      maxSize: 3em
      minWidth: 300px
      maxWidth: 900px
    }
  }
}
//yml end

.test
  ctr({
    width: 200px
    line-height: responsive
    'comp-span': {
      line-height: responsive
    }
  })

ctr('.test', {
  width: 200px
  line-height: responsive
  'comp-span': {
    line-height: responsive
  }
})
