__Description__: Same as basic but using the object key of `customNos` and `customNots`, `customNos`

__Notes__

+ regex: ` /^nons$|^nots$|^customNos|customNos|^customNons|^customNots/`
+ can also have keys like `customNonsBeer`
