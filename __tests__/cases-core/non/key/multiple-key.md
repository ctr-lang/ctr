__Description__: If the `key` value pair is a `list` consider each `list` item as a separate `non` entity and process it individually.

__Notes__

+ A Stylus `list` is the equivalate to a Javascript `array`
