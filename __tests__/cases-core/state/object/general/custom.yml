# File generated from ---> custom.styl


.test:::0:
  width: 200px
  customStates:
    hover:
      on:
        background: red
      non:
        background: green
    active:
      # auto gen
      color: red
    random:
      on:
        font-size: 10px
      non:
        font-size: 20px
      option:
        # will be key
        key: 'link'


.test:::1:
  width: 200px
  customStatesBoooze:
    hover:
      on:
        background: red
      non:
        background: green
    active:
      # auto gen
      color: red
    random:
      on:
        font-size: 10px
      non:
        font-size: 20px
      option:
        # will be key
        key: 'link'


.test:::2:
  width: 200px
  customSts:
    hover:
      on:
        background: red
      non:
        background: green
    active:
      # auto gen
      color: red
    random:
      on:
        font-size: 10px
      non:
        font-size: 20px
      option:
        # will be key
        key: 'link'


.test:::3:
  width: 200px
  customStsBoooze:
    hover:
      on:
        background: red
      non:
        background: green
    active:
      # auto gen
      color: red
    random:
      on:
        font-size: 10px
      non:
        font-size: 20px
      option:
        # will be key
        key: 'link'

