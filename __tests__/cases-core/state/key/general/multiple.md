__Description__: If the `key` value pair is a `list` each `list` item should be considered as a separate `state` entity and processed individually.

__Notes__

+ A Stylus `list` is the equivalate to a Javascript `array`
