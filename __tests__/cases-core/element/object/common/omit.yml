# File generated from ---> omit.styl


.test:::0:
  width: 200px
  elements:
    #  merged into each element
    common:
      font-size: 1em
      border-radius: 50%
      hover-on:
        opacity: 0.5
        font-size: 2em
    before:
      content: 'before'
    first-child:
      option:
        # omit
        omit: ['border-radius', 'hover-on.font-size']
      color: red
    random:
      option:
        #  will be key
        key: 'nth-child(2)'
        omit: 'border-radius'
      color: black


.test:::1:
  width: 200px
  elements:
    #  merged into each element
    common:
      font-size: 1em
      border-radius: 50%
      hover-on:
        opacity: 0.5
        font-size: 2em
    before:
      content: 'before'
    first-child:
      option:
        # omit
        omit: ['border-radius', 'hover-on.font-size']
      color: red
    random:
      option:
        #  will be key
        key: 'nth-child(2)'
        omit: 'border-radius'
      color: black

