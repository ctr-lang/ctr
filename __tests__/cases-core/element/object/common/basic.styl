.test
  ctr({
    width: 200px
    elements: {
      // merged into each element
      common: {
        font-size: 1em
      }
      before: {
        content: 'before'
      }
      first-child: {
        color: red
      }
      last-child: {
        color: blue
      }
      random: {
        option: {
          // will be key
          key: 'nth-child(2)'
        }
        color: black
      }
    }
  })


ctr('.test', {
  width: 200px
  elements: {
    // merged into each element
    common: {
      font-size: 1em
    }
    before: {
      content: 'before'
    }
    first-child: {
      color: red
    }
    last-child: {
      color: blue
    }
    random: {
      option: {
        // will be key
        key: 'nth-child(2)'
      }
      color: black
    }
  }
})
