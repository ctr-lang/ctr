__Description__: Select all children after the first to **n**

__Notes__

+ Parameters
    * `after-first(num)`
    * `fm-after-first(num)`
    * `afterFirst(num)`
+ `fm` prefix to avoid collisions
+ `<elm>:nth-child(n+${num + 1})`