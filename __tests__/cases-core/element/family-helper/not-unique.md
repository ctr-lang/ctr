__Description__: Will only select children if they are not unique. Meaning if there is at least 2 children, the style is applied

__Notes__

+ Parameters
    * `not-unique()`
    * `fm-not-unique()`
    * `notUnique()`
+ `fm` prefix to avoid collisions
+ `<elm>:not(:only-child)`