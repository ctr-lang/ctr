__Description__: Select all children before **n** from the last

__Notes__

+ Parameters
    * `from-end(num)`
    * `fm-from-end(num)`
    * `fromEnd(num)`
+ `fm` prefix to avoid collisions
+  `<elm>:nth-last-child(${num})`