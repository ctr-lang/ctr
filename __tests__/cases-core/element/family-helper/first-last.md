__Description__: Select only the first and last child

__Notes__

+ Parameters
    * `first-last()`
    * `fm-first-last()`
    * `firstLast()`
+ `fm` prefix to avoid collisions
+ `<elm>:first-child` && `<elm>:last-child`