__Description__: By default `ctr` should convert `content` values into strings, although, if the user wished to ovewrride this default behaviour they can pass `stringify: false` in the `option` object

