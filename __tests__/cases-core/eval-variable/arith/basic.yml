# File generated from ---> basic.yml.styl


.test:::0:
  $$:
    base: 10
    mult: 2
    eval:
      #  10 * 2 = 20
      # need to append px
      width: 'arith($base$ * $mult$)px'
  width: '$eval.width$'


.test:::1:
  $$:
    base: 10
    mult: 2
    eval:
      #  10 * 2 = 20
      # need to append px
      width: 'arith($base$ * $mult$)px'
  width: '$eval.width$'

