# File generated from ---> multiple.styl


.test:::0:
  $$:
    base: 10
    mult: 2
    eval:
      #  10 * 2 = 20
      # need to append px
      width: 'arith($base$ * $mult$)px'
      height: 'kalc(100% - $eval.width$)'
  width: '$eval.width$'
  height: '$eval.height$'


.test:::1:
  $$:
    base: 10
    mult: 2
    eval:
      #  10 * 2 = 20
      # need to append px
      width: 'arith($base$ * $mult$)px'
      height: 'kalc(100% - $eval.width$)'
  width: '$eval.width$'
  height: '$eval.height$'

